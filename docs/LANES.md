# The lanes — one label per thread of work

Kevin, 14 Sep 2026: "should we split these tasks and label them easier to
follow." Yes. Every brief, commit and message uses one of these labels so
you can tell at a glance which thread a thing belongs to.

| Lane | What it is | Where it stands (14 Sep 2026) | What it needs from Kevin |
|---|---|---|---|
| **LEADS** | Kevin, 17 Sep morning: "Jess starting on Monday scheduling all leads in the new app… we have to start here, we're not going to do the first redundancy… everything she was doing in Contractors Cloud we are already doing or it's being done for her." The first door the office walks through ahead of CC, from **Mon 21 Sep**: the New lead door (`docs/JESS-MONDAY.md`, the film `?tour=leads`). | **Live, 17 Sep, v103 + migration 381_the_first_piece_leads.** `job_create` v2 (p_appt_minutes, p_what): the rep's phone buzzes the moment the estimate is booked (day · time · address · what they want · who booked it), the office seat hears about a lead with no rep, the booking is the file's first line; the door shows the rep's day so nobody is double-booked; the file's header carries ESTIMATE · day · time · rep and the CC chip; NEXT says booked-with-whom, or "no appointment yet, call and book" in red. **The copy into Contractors Cloud:** `cc_mirror_queue` (one row per lead born here, CC's own ids in the payload), `job_link_cc` re-keys the job · thread · appointment · customer onto CC's ids (two FKs now cascade on update), `cc_mirror_mark` (Typed into CC · queue again · not for CC), worker `backend/worker/lead-mirror.mjs` (POST bodies are a stated GUESS until the first real row; needs a WRITE-scoped CC token on the box), all behind switch **`cc_mirror` OFF** — OFF the file says NOT IN CONTRACTORS CLOUD YET and Copy for CC hands the office the fields in CC's order. `lead_create` (the web door) queues the mirror too. Jess's page: `docs/JESS-MONDAY.md`. Email to Jess (cc Laura, Sam) drafted, not sent. **384 (the same morning):** `job_appt_set(job, at, minutes, note)` — Change the time · Cancel the visit · Book the time on the file: the rep buzzed, the line on the file, the visit row moved or cancelled CC's way (the CANCELLED title), and a follow-up `cc_mirror_queue` row (kind reschedule · cancel) for a lead already in CC, which the worker PATCHes when the switch is on; the file's chip says THE NEW TIME / THE CANCELLATION … until then. | Send the Jess email (schedule it for 8 AM if it is after 7:30 PM). Say whether the box's CC token may write (or mint one); then `lead-mirror.mjs --dry` on the box, one real row with `--force`, then flip **New leads → Contractors Cloud** in the Office room. Flip `appt_confirm` for Fencing if he wants the customer texted on booking. Whether Laura and Sam start Monday too (they book most Fencing appointments today: Laura made 4 of the last 5). |
| **CONTRACT SIGNING AND AUTO WORKFLOW** | Kevin's session name, 16 Sep evening. The customer accepts now and signs every form once (the permit application and the hold harmless included, nothing notarized on the link; Oasis signs the contract alone). The Notice of Commencement is the customer's errand: emailed filled the moment they sign (the rep and the office copied), signed before a notary, a picture from the link closes it; the machine texts them until it lands (gentle for fence/Oasis, hard for roofing, rows in `noc_nudge_plans`); the rep never goes back. | **Live, 16 Sep evening.** 367 (`noc_handoffs`, `noc_nudge_plans`, the handoff on the fill, `noc_nudge_sweep` every 30 min, the chain no longer waits on the NOC) and 368 (Oasis: the contract alone, `paperwork_forms.except_brands`) applied; edge fn `noc-return` (the customer's photo page) deployed; the file's Paperwork card shows the NOC row (Email it · Copy the photo link · Resend), the Office room has the switch **The NOC to the customer** (OFF), the Ride-Along `?tour=noc`. Walk-through file: "Delaney NOC walk, Kevin". Owed: the "received the terms and conditions" initial on the Oasis sheet (e.html); the per-form yellow spots on the real page; Oasis and Pro-Tech texts wait on their 10DLC lines. | Both done 16 Sep ~10:45 PM on his word: **The NOC to the customer is ON**; the note to the sales team went out from his Gmail. **~11 PM, "nothing holds it up":** 369 — the contract settling opens PERMIT on Sam and MATERIAL on Jonathan at once, every brand (roofing's intro call too); a calculator job releases its material at the signature (custom/aluminum once the deposit is in); `machine_email_watchers` (Kevin, Jess, Gio) ride every machine email; the office and the rep are pushed the moment the NOC goes out; the SIGNED note rewritten ("it's moving": what opened on whom, to the rep, Gio, Kevin, Jess, Sam, Jonathan). 370 — the handoff opens at the signature and the form rides in when the fill lands (Kevin's own walk-through went quiet when the county did not match his address); the office is nagged at 30 minutes if nothing filled; parcel-lookup fills the NOC even with no county match. **~9:30 PM, "back to our original move" after the fine print (the hold harmless is a condition of permit issuance for a fence in an easement; Rockledge and Orange City ask for the recorded NOC at intake):** 376 — the chain is back to 340 (every paperwork ask settled → PERMIT; paperwork official → material); the SIGNED note is the visit note again with the notary packet attached (373 fills the town's forms on the lines); the customer's note is "you're in — approved and moving — two forms to sign before a notary — the rep sets a time, or any notary and a picture"; the texts say the same. Kept: watchers, Luis, the photo page, the on-the-lines fill, the survey fix. **~10:45 PM, 378:** the terms clause is live (version 2026-09-16-formalities, all five terms pages); the counters' fine print is rows in `permit_jurisdiction_rules` and a line on the Property card; the Oasis link carries the "received the terms" initial; the MATERIAL ask has Send to supplier (switch The order to the supplier, OFF). **17 Sep morning, 385 — FINAL:** everything signs on the link, hold harmless included, every county; the NOC is the rep's (out of the customer's process — switch OFF, no handoff on the fill; the ask on the rep; reminders on days 2/5/9/14/21/30 in `noc_rep_nag_steps`); nothing waits — PERMIT and MATERIAL open at the signature (chain back to 369). The appointment-confirmation text now asks for the survey. **386:** the warranty deed — a mismatch or an entity owner on a signed job emails the customer for a picture of the deed (photo link, Sam and the rep copied), texts until it lands, the deed ask on the office holds nothing (switch `deed_request` ON; Request the deed on the Property card). Owed: the per-form yellow spots on the real page; Oasis/Pro-Tech texts wait on 10DLC. **17 Sep, from the office's mail:** (1) the `noc-return` photo page (the NOC and the deed pictures) is served as plain text — Jess, 8:23 AM, "the stamp page opens to this" — because the gateway sandboxes HTML from every function on this project (`Content-Type: text/plain` + a sandbox CSP, MCP deploy or CLI alike; estimate-view's 404 page shows the same), so the page has to become a static file on github.io that calls the function for JSON, the way w.html and e.html do; (2) Sam on the filled NOC: the wording overlaps at the bottom of the form (the clerks may reject it); the scope on line two should just say "Fencing", "Re-roof" or "Pavers"; the name at the top of line three must be the signer — with a POA keep the homeowner's name there, the signer adds "POA" / "Authorized Agent", and the proof of the POA goes on the file with the packet. |
| **SIGN** | The estimate the customer taps ACCEPT on — Mike's Billdu flow on our own page (322). | Live. Customer page in Billdu's order (summary card → ACCEPT → the document). The builder is on the customer file in Central Command (the **Estimate** button). Phone-app builder next. | Walk it through on a test customer; say what feels different from Billdu. |
| **PAPERWORK** | The contract + disclosures under one signature — the DocuSign replacement (319–321). *16 Sep evening: superseded by CONTRACT SIGNING AND AUTO WORKFLOW above — everything signs on the link, the NOC goes to the customer.* | Built, `esign_packet` switch OFF. Contract at signing; disclosures on the permit run; Oasis contract only. License line prints per brand (323). | "approved" on the four terms drafts → the version bumps and the drafts go live. |
| **PERMIT** | NOC and permit forms filled from county data. *16 Sep evening: the permit forms sign on the link; the NOC is emailed to the customer to notarize (CONTRACT SIGNING AND AUTO WORKFLOW above) — nothing is signed in person any more.* | **Lookup live (324):** the Property card on every file asks the county and shows owner of record, parcel, legal, mailing address and the signer check. Five counties answer from our server (Brevard via the county GIS layer, Orange, Volusia, Seminole, Flagler); Indian River waits on its weekly file. The lookup fires by itself when a customer taps ACCEPT. **The NOC fills itself (325):** the moment a customer taps ACCEPT the lookup runs and the NOC is made behind it, no tap needed; the Property card's "Fill the NOC" button remakes it any time. It makes the county's own form for Volusia and Flagler and the statutory NOC for Brevard, Indian River and Seminole, filled with owner of record, parcel, legal, mailing address, improvement and the contractor block; it lands on the file with an Open link and a list of the blanks the office still types (permit number, surety, lender, expiration, signature, notary). Next: the permit applications and hold-harmless forms per city, then in-person signing on the rep's phone. Regrid rejected ($376 a month, trial excludes our counties). | Indian River only: one weekly download of the appraiser's export. Later: e-recording and online notary decisions. |
| **FENCE PACKET** | Gio's calculator inside the rep app does the office's breakdown: the rep draws or types once, and the material order, the signed proposal, the county packet and the drawing land on the file; a signed proposal emails the packet to Sam, Laura, Jess, Kevin and Gio with every file attached (328, 14 Sep). | **Live.** The customer file here shows **The fence job** card: the six numbers (style, feet, gates, tear-out, core drill, grade), the county's description of work ("INSTALL 4' TALL ALUMINUM FENCING (177 LF) WITH (2) 3' GATES"), the material order Jonathan orders from, and the files with Open (ten-minute signed links into the private bucket). The NOC fill reads the same row (331 → paperwork-fill). **The estimate autopopulates (14 Sep, v26):** Estimate on the file, or "Estimate from this" on the fence card, opens the builder already typed from the takeoff: one line per style with its feet, the calculator's quote as the job price, gates and site work in the scope; the rep reads it and taps Create. **The job reopens on any device** (app PR, rides the next OTA): the calculator asks the file for the saved job before it loads and restores it through Gio's own Load Job routine, so a yard drawn in Safari on the iPad is on the phone and on the office's screen with nothing redrawn; a job saved on that device wins. Next: the four questions after Complete Quote (owner on deed · easement · survey photo with the fence drawn on it · pool/HOA · financing), then the order to the manufacturer once Jonathan's step is known. Kevin's rule from the packet read: the survey line stays a human's, the notary stays a human's; everything else on the twelve pages is the machine's. | Gio's answers: the corners box, the survey as the drawing's background, Jonathan's supplier step. One line in his page (a send to our door) so the reps who still draw in Safari land the job on the file too. |
| **THE CHAIN** | Turned in → Sam → Jonathan → Luis → Laura with no human deciding (340, 15 Sep): a link signature opens the checklist and settles the contract; the last upload is paperwork official; Palm Coast settles its own permit step; material release opens MATERIAL for Jonathan with the order attached; the date opens COMPLETION_SIGNOFF for Luis. The Flow room shows all of it scrolling. The locate (341): the ticket is written from the file for Sam to paste; the Exactix confirmation and each utility response close the step by themselves. | **Live** (340 applied; probes on the Cocoa test file and a Palm Coast address walked signature → Luis). The chain's customer texts (permit_in, schedule_set) are drafts behind the switches. | The first real job through it; Jonathan's PO step and the supplier send (4d). |
| **FILES** | The customer file as the hub: text · tag · document · send to · invoice · collect · estimate. | Live in Central Command. CC files pull waits on `--apply` (Kevin runs it). | Run the CC files pull commands. |
| **OFFICE DAY** | The migrated CC workflow, the village rooms, the six switches. | Built beside CC, every switch OFF. | Flip switches one at a time when ready. |
| **COCKPIT** | Reps text from inside the app: one screen, text · email · call, the reply lands in the file and buzzes the phone. | Fencing live since Sat 12 Sep. Oasis and Pro-Tech by Tue 15 Sep. The door is renamed ENTER THE COCKPIT in code (ships with the next OTA). Tonight's post + simple email and the morning graphic email + post are drafted, nothing sent. Weekend: every reply pushed (2 of 2 devices for Haakon); Mike's Friday bell post is the one open glitch. | "push it" for the OTA; "send" on the emails; the go for Oasis/Pro-Tech. |
| **SWITCHBOARD** | One place every word lands: the customer's texts, the office's notes, the rooms, the direct lines and the subs in one rail that never closes, plus the board where nothing goes unanswered (`docs/SWITCHBOARD-SCOPE.md` — with every linked conversation and Jess's team's needs). | **The Line is live on main (v39→v46):** the rail, where-it-is-stuck, the most-asked question, nothing-goes-unanswered, **`@` in every room** (v48 — `@Samantha call this customer Mark Whitfield`: people from the seats, customers by name / street / phone, the post lands on the file too, a Text-the-customer lane), **SAY IT** (v47 — to any person or the customer, about any customer picked by last name / address / phone, Jess's 308 office lines as presets), and **the lane** (Jess, 14 Sep: "I am still getting every text from sales" — an office seat lands on its own work, Everything one tap away). Direct lines + the Message-anyone box (⌘K) are LIVE — 346 applied 15 Sep ~21:20 UTC (dry-run in rollback, verified). Sibling picture: `docs/jetstream-on-steroids.html`. | Apply 346 (check the live registry first). Repeat the two or three things Jess said so they go at the top of her section. Then: one composer two lanes → the rail on every screen → the manager loop + Listen as rows → the outside line. |
| **THE OUTSIDE WAIT** | HOA approval and the permit — the two bottlenecks Kevin named, 15 Sep. Both are an ask on somebody who does not work for us (`docs/THE-OUTSIDE-WAIT.md`). | **Designed, not built.** The finding that drives it: an HOA is not slow, it is *periodic* — miss the submission window by a day and it costs a month, so the meeting calendar has to be data before the packet is built. Permit is a queue, so it gets a different treatment: completeness checks off real rejection reasons, a named human per department, and status polling so nobody has to ask. Needs `hoa_associations`, `third_party_submissions`, and 324's jurisdiction rules extended; the nurture engine (226) already does the sending. | Whether to build it, and the words for any sequence that emails an association (the email-launch gate binds). |
| **BILLS** | Every supplier invoice lands on the customer file by itself, Jonathan approves in one tap, and the bill goes to Contractors Cloud and QuickBooks without a retype (`docs/THE-BILLS.md`, 16 Sep). Today: the supplier emails Jess, Jess forwards Jonathan, Jonathan types it into CC, Claudette pulls the same invoice off the portal and types it into QuickBooks; Ref # blank and QB sync null on every recent CC bill. | **Scoped, not built.** The Billtrust suppliers (ABC Supply, SRS, Heritage) already email a PDF + an IIF, so the parse needs no OCR; the PO matches `cc_material_orders`; our own QuickBooks door (018, `qb-client.mjs`) is written and waits on Kevin's consent per company. Census email to Jess + Jonathan + Sam + Laura drafted, not sent. Afternoon: the other direction scoped too — INVOICE READY on sign-off, typed from the signed estimate minus deposit plus change orders, photos and Luis's sign-off beside it, Approve or Hold (§7); the reply on Jess's ABC thread, the all-hands "don't get overwhelmed" note and Jonathan's new-role note drafted, not sent; Jonathan's Ride-Along built from the real screen with a mounted Bill landed card; Laura and Jess's invoice film the same way; Laura's note drafted. Gospel 33 written (nothing to learn, it comes to you) and every note, film and page ends on it. Kevin, 3:50 PM: "turn everything on" → the backend build started on the Commercial-Desk branch claude/jake-sales-virus: **migration 365 applied on live at 17:25** (supplier_bills · v_bills_queue · bill_decide · switches bills_to_qb, bills_to_cc OFF), the IIF parser tested green, the intake and QuickBooks workers written but never run; QuickBooks is connected for 3 of 4 companies on live; the front end (js/bills.js) not started. Kevin moved accounts at ~4 PM: **read `docs/HANDOFF-16-SEP.md` first.** **Evening, the first spoon is built, every switch OFF (v96):** `js/bills.js` puts two cards on the customer file and a fifth tile + the queue in the Office room. **Invoice ready** on the office seat (Kevin: "however they send them out of Contractors Cloud, it needs to be just as easy through here"): opened by the sign-off or the INVOICE ask; typed from the accepted estimate or the sold amount, minus the deposit on `fence_jobs`, plus signed change orders; the photos against the proof rule, the sign-off, the customer's last text and the brand's "invoice sent" line beside it; red before anyone opens it when photos are short, a change order is unsigned or there is no sign-off. **Approve** = `invoice_request` + an optional invoice number that settles the ask + the text from the main line, one press; **Hold** tags the supervisor or the rep with the reason. **Bill landed**, one per `supplier_bills` row: supplier · invoice · PO and the order it found · amount beside what we ordered at · due · lines; red when over the estimate, no job matched, unreadable or past due; Approve · Wrong job · Hold · Pick the right file · Open the PDF; approved with the switches OFF it hands the office the fields in CC's order with a Copy button. **Migration 369 applied on live** (dry-run first): read policy `is_office() or is_manager()`, `wrong_job` stays on `v_bills_queue`, `bill_rematch(p_id, p_customer)`, `bill_decide` guarded like `invoice_request`. Worker deps installed under `backend/` (own `package.json`), IIF test green; the workers still never ran: they run on the box, not this PC (no service key here). Five Gmail drafts still unsent. Nothing switched over. | Say "label the inboxes" or give one `bills@` per brand; make the Google app password and type it into the box's env yourself (never in chat); the Liberty Roofing QuickBooks consent, after checking whether 1537 already shares Pro-Tech's company (the 1563 row is labelled "Liberty Roofing Group Inc. DBA Pro-Tech Roofing", multi-brand); send the five drafts; decide CC's own QuickBooks bill export; say whether Gio (role owner, so `is_manager()` lets him read bills) should be cut out of the books at the database too. |
| **INVOICE** | The customer's invoice, from LISTO to the money, with no person typing (`docs/THE-INVOICE.md`; the one-page for Jess and Laura `docs/the-invoice.html`). Kevin, 17 Sep: "is it going to be auto created when the guy says it's complete? So we remove that piece completely… send the invoice to the text number and the email… politely bug them… if they don't within a few hours, we'll call them… where do we put any change orders?" Today (read from QuickBooks 17 Sep): 119 invoices in 30 days on the fence company alone, every one typed by hand as one line "New Fence Installation" on a customer named "job number - street", due on receipt, no email on the customer so QuickBooks never sends it, no pay link. | **Built 17 Sep, every switch OFF.** Migration **381 applied on live** (dry-run rolled back first): `invoice_build` (the math from the file: the accepted estimate or the sold amount − the deposit taken + every signed change order; the sign-off's photos against the rule; the problems), the change order as an estimate doc the customer signs on the same link (`estimate_docs.kind`), the switch **`invoice_auto`**, `invoice_nudge_plans` (30 rows: text +2 h · call card +4 h · day 1 · 3 · 7 · 14 · 21 · past due 30), the **COLLECT_CALL** ask (the call card on the invoicing seat, red at 4 h), `invoice_sweep` every 15 min (builds what is clean, nags once on what is not, walks the plan, pauses on an unanswered reply, keeps quiet hours, marks paid from the QuickBooks copy), the worker's doors `invoice_sent_mark` / `invoice_paid_mark` / `invoice_balance_mark`, `invoice_state` for the card. The QuickBooks writer `qb-invoices.mjs` (rail 2 moved out of `qb-bills.mjs`): customer named Jess's way, the lines, DocNumber = the CC job number, due on receipt, the company's item learned from its own invoices, the pay link read back, the PDF on the file, QuickBooks' own email, the balance read back; on the box as `invoice_out` every 15 min. **v102:** the Invoice card (`js/invoice.js`) in six states with the math, the lines, the diary and the plan; Approve (a reason past the problems) · Hold · Paid · Stop the reminders · Text the link again · Open the PDF; the **Change order** button on every signed file; the Office room's *Calls · invoices* tile and the two switches. Demo: `?demo=1&as=office&file=cj3` (clean, goes by itself) · `cj11` (sent, the call card on Laura) · `cj5` (red). | Flip **`qb_invoices`** on one real sign-off with `invoice_auto` OFF (Laura presses Approve, checks the invoice and the email in QuickBooks); then **`invoice_auto`**; the texts ride `office_machine_texts`. The hours in `invoice_nudge_plans` (rows). The deposit on the invoice as a minus line (Billdu's way, today) or the full amount with the deposit as a QuickBooks payment (Claudette's call). The Ride-Along for the new card (the 16 Sep film shows Approve). |
| **HAND IT BACK** | Everything the office types into Contractors Cloud by hand, handed back as one card with one decision (`docs/HAND-IT-BACK.md`, Kevin, 16 Sep (evening): "I don't want them to say 'what do I do?'… take whatever they did and hand it back to them to just make the decision"). Measured 16 Sep (evening): every CC task the office closed in 90 days came from a template (0 typed by hand); what Jess types is the money — subs, crews, fees, the invoice, the payments — none of it mirrored. | **Modelled and started.** Migration 376 on live: `payees` (who bills us, the kind, how it arrives; seeded from Jess and Sam's 16 Sep answers), `supplier_bills.kind` (supplier · sub · crew · fee), the paper door `bill_land_by_hand`. The card says its kind (v97). The intake worker lands a known payee's PDF. No new switch; `bills_to_qb` · `bills_to_cc` still OFF. | No mailbox password any more: the intake reads through the Workspace delegation key (16 Sep late). Kevin: the one read-only reach check on the box, the two drafts (Brian at Heritage, Jess on Havana · Home Depot · Merchant Metals), then the supervisor app's TAKE for the crew's paper (next spoon). |
| **THE OLD KNOWS** | The transition is by job, not by date: every job signed on the link lives here from birth, and the old system is fed by the machine, not the people (Kevin, 17 Sep: "as soon as they do the new way is everything going to stay within our ecosystem"). A settled ask ticks its Contractors Cloud task with a comment (who, when, the input); the steps the chain passed are ticked as passed; the money steps (upload contract and sales order, record NOC, invoice, close-out) get a CC project note and stay the office's until their lanes are on. What each step gets is a column on the workflow map (`cc_workflow_steps.mirror`: tick · note · passed · skip), rows Kevin and Jess edit. | **Live 17 Sep, switch OFF.** 382 `cc_task_mirror` + trigger on the settle; worker `backend/worker/cc-task-mirror.mjs` on the box every 5 min (`liberty-cc_task_mirror.timer`); the box's CC token writes (proved: probe task 10435066 on Kevin's CC list). 383: the supplier order from the MATERIAL ask carries `PO <job number>` and the bills intake matches a bill's PO to the job number, so no CC material order has to be typed for a bill to find its file. Still by hand until their own lanes flip: the sales order and job costing in CC, the customer's invoice and payment (the INVOICE lane, 381), bills to CC and QuickBooks (`bills_to_cc` · `bills_to_qb`). The other direction (a lead scheduled here becomes a CC project) is the LEADS lane's `cc_mirror`. | Flip **Asks → Contractors Cloud tasks** in the Office room when the first link-signed job is in; the held rows go then. Say whether "Record NOC" should tick on Sam's recorded upload instead of a note. |
| **CREWS** | No app, no login: one text with one link per job, and the link is the room, a mini chat for that job: the crew, Luis, the supervisor, every tap and photo on the customer's file (`docs/THE-CREWS.md`; Kevin, 16 Sep 9 PM: "problem solved. no app… like send mini chat ecosystems"; his 7:30 "one login per crew" withdrawn). Luis meets the crews Wed 17 Sep to show them what they need. | **Already built (342 · 347), one switch OFF (read from the live functions 16 Sep night).** `crew_link_text`: the moment an install date is set, the crew lead's phone gets the work order link by text from the brand's line, Spanish first; OFF = the link goes by email only. `crew_link_view`: the link page shows the file's SUPER lane (the supervisor's words and the crew's own), never the customer's texts, never the office lane. `crew_link_event`: AQUÍ · FOTOS · LISTO · a message land on the file with the photos and push the supervisor. Live tonight: 37 crew cards in Contractors Cloud, 8 with a phone; the 12 Aug crew seats inactive with no phones; one work order ever, never sent, two test messages on its link. **v99:** the switch is in the Office room's list; the handout (`the-crew-day.html`, ES/EN PDFs) and Luis's handbook (`how-it-works-luis.pdf`) reprinted around the link, no login anywhere. **~10:30 PM, migration 379 on live (Kevin: "go on all but don't send anything until morning"):** a reply to the text itself lands on the job's file with the crew's name and pushes the supervisor (no switch; it only lands what comes in); the text back to the crew lead when anyone writes in the crew's lane; the morning-of text at 7 AM Florida on the work date; FALTA MATERIAL as one red button on the link → a material ask on the supervisor with the clock. **v100:** @crew in the note box on every file, so anyone's note reaches the crew's lane. Every crew page opens in the phone's language with two big ESPAÑOL / ENGLISH buttons (never assume Spanish). All outbound behind `crew_link_text`, OFF. Next: the roster with phones → flip the switch → the first real job → translation both ways (`crew_translate`) → the nuggets on the same page → the Spanish film. **Sent 16 Sep ~8 PM on Kevin's word:** the nine questions + the handout (page, ES and EN PDFs) to Luis, cc Jess; "Your supervisor app: the two-minute film, and how it gets on your phone" to Luis, Obed, Robert and Mike, cc Jess (the film, the site on the phone, TestFlight in two taps once Apple's invite lands, the ticket rule). | **The roster is in (17 Sep, 388):** Luis's nine crews with the lead's cell on the crew seats and on his own list on The Line (`crew_people`), Spanish for all, Robert on the roofing crews; `crew_link_text` reads the roster by name as its third source. Two numbers to confirm with Luis (Gregorio …9932 vs …9982, Millenia 487 vs 467-6112; the email won) — the reply is a draft in Kevin's Gmail. Then flip **The crew's text** in the Office room, fence crews first (the Oasis and Pro-Tech lines are not campaign-approved, so their crews would be texted from the Fencing line); Luis's LISTO checklist on the crew page (permit posted · company sign · fence and gates top to bottom · the check); the tutorial morning at the yard with the crews present (Luis asked); translation on or off at the start; the Spanish narrator (Jorge, free, licensed like GUY). |
| **ONBOARD** | A new rep's first day: login, the file, the cockpit, the receipts, the paperwork — expectations for everything besides selling (the Tim lesson). | Gustavo starts Mon 14 Sep on Oasis landscaping. Runbook exists (B2B-COMMERCIAL-SCOPE §7); the day-one expectations piece is not written yet. | Gustavo's Oasis mailbox and CC user; then the runbook runs. |

Rules that hold in every lane: every outbound text/email is a draft until
Kevin says send; signing is not selling; never edit a published terms
version in place; nothing is switched over from Contractors Cloud until
Kevin says so.
