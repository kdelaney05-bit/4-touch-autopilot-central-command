# Hand-off · Saturday 19 Sep 2026, 8:30 AM ET · the account switch

Kevin is moving to a new Claude account this morning ("this one is out of
Fable"). A Claude Code session, a routine and an artifact belong to the account
that made them and do not cross. The repos, the live database, GitHub and
Kevin's Gmail cross on their own. So, as on 14 and 16 Sep: everything durable
is written here, and the pieces that live only on the old account are listed
so the new one can remake them in an hour. Nothing in this file is a customer.

## Where everything is

| Piece | State at 8:30 AM ET |
|---|---|
| Central Command (this repo) | `main` at v147 (THE HANDOFF). Push = deploy. Working tree here clean. |
| trureview-mobile | `main` at `85d3bce` (the 18 Sep evening board line). 16 open PRs, table below. |
| liberty-command | untouched by the sessions of 18–19 Sep except the branch `claude/60-day-company-summary-x3k35d` (the reports session). |
| Live database `schema_migrations` | top is **417** `417_the_handoff` (416 + 417 applied 19 Sep ~3 AM). **Next free = 418.** 415 `415_the_hunter` is written on PR #433 and **not applied** (its tables are missing on live). 406 is live and still unregistered. |
| The films | `docs/ride-alongs.html` newest is Tag the office (18 Sep). |
| Kevin's PC clone of this repo | eight bridge sessions report it **dirty and mid-rebase** at `3571087` (v145), and two archived sessions report **one unpushed commit `43705a1c`** at 18 Sep ~9 PM. That commit is not in any of main's 370 commits. Finish or abort the rebase on the PC (`git status`, then `git rebase --abort` or `--continue`), then look at what `43705a1c` holds before pushing or dropping it. |

## What dies with the old account, and what to do about each

1. **The routines' host session** ("60-day company summary report",
   `session_01QgXEuLGosaQdcSBAoM9czJ`) is blocked since 7:21 AM ET on a
   permission prompt for a read-only Supabase query. The Hunter fired into it at
   6:39 and the Mouse Trap at 7:08; neither brief was produced. It cannot be
   approved from another account. Let it go.
2. **The six routines** are bound to that session on the old account, and they
   store no connector grants. Re-create all six on the new account from the
   claude.ai routines page **with the connectors attached** (Supabase, Windsor,
   Gmail, Google Calendar, QuickBooks, HubSpot). The verbatim prompts, the
   schedules and the account ids they use are in Kevin's Gmail Drafts under
   **"The six routines, verbatim · 19 Sep"** (Gmail crosses accounts; the
   prompts carry ids that do not belong on a public page). The schedules:

   | Routine | Fires (ET) | Cron (UTC) |
   |---|---|---|
   | The Hunter | daily 6:30 AM | `30 10 * * *` |
   | The Mouse Trap | daily 7 AM | `0 11 * * *` |
   | The Monday Ads Sheet | Mon 7 AM | `0 11 * * 1` |
   | The 60-Day Catch-Up | Mon 8 AM | `0 12 * * 1` |
   | The Report Card | Mon 8:30 AM | `30 12 * * 1` |
   | The Feeder | Tue + Fri 7:30 AM | `30 11 * * 2,5` |

   The first firing of each on the new account is its proof. Once they run
   there, delete the six on the old account so two Hunters never both propose.
3. **The three artifacts** (The Monday Ads Sheet, The 60-Day Catch-Up, The
   Hunter blueprint) are private pages under the old sign-in. The Monday
   routines on the new account will publish new editions at **new URLs**. The
   **Reports card on page one of The Business** (`js/home.js`) links the old
   Catch-Up URL; re-point it the first Monday the new edition exists
   (docs only, but it is app code, so cache-bust).
4. **Five bridge sessions** on Kevin's PC sit blocked on "say send" for Gmail
   drafts. The drafts themselves are in Gmail and cross; the sessions do not.
   The eight unsent drafts (all 18 Sep): the team note on the reps' rooms
   (v143–v145); the Village-as-conversations note; the reply to Sam on Sunbiz
   first; the reply to Jess on the rebuilt contract; the write-up to Gio and
   Jess on this week's "call the office" case (written before **Hand off ▸**
   existed, so its fix still says Tell the team); the reps' new call process;
   the forward-when-offline ask to Dwayne; a recap to Kevin himself. Send,
   rewrite or delete from Gmail directly.
5. **The usage window.** 26 of the last 40 sessions on the old account carry a
   seven-day overage status of "rejected", resetting Wed 23 Sep 5 PM ET. That
   is why the account is out of Fable; nothing to do but move.

## Open pull requests, checked against live

**This repo.** PR #1 (warranty deed, 14 Sep, built on v26) and PR #2 (Uvoice
handoff docs, ledger at 366) both conflict with main. The deed work shipped
another way (the `deed_request` switch, 386 and 413). Both are close
candidates; nothing in them is needed.

**trureview-mobile (16 open).**

| Group | PRs |
|---|---|
| 19 Sep, clean, need Kevin | **#434** The Handoff: `npm run typecheck`, then the OTA for the phone's Hand off chip. **#433** The Hunter: apply 415 (read `schema_migrations` first), `WINDSOR_API_KEY` into the box env, `--dry` run, enable the timer, approve the three cap rows. |
| Applied live, only unmerged | #431 (407), #428 (395), #423 (346 ledger), #422 (345 is registered live although the PR text says not applied), #420 (itb-reader, on the box), #418 (340 + 341), #417 (339), #395 (327 + 329; the app half never published), #424 (348, stacked on #418). Merge in that order; #424 after #418. |
| Waiting on Kevin's decision | #427 Jake's desk, #421 the CC API repoint, #404 the fence calculator OTA, #394 the 14 Sep account-switch doc, #388 (305 is not applied). |

## Owed under the film law and gospel 36

- **Films not made:** the Handoff (v147), Google Calendar (404), Who's free
  (407). The maker is `Commercial-Desk/tools/ride-along/make.mjs` on Kevin's PC.
- **Two 8 AM pushes** (the words-first film, the Library film) were marked not
  sent on the 18 Sep brief. There is no push log table on live, so whether they
  went since cannot be read from the database; the SQL is in `docs/MORNING-BRIEF.md`.
- **Two one-line fixes still open**, both migrations: `nurture_merge_ctx` still
  greets by `split_part(name, ' ', 1)` (verified live at 8:29 AM), so a
  "Last, First" customer is greeted by the last name; the file thread paints
  a text reply as an email reply because the engine's `reply` event carries
  no channel (server-side; not verifiable from this repo).
- **Rows for Kevin and Jess:** the five `office_lines` rows `handoff_*` run
  25–34 words (schedule is 34; gospel 38 asks for about 30). `reps.counties`
  is set for Eric, Haakon and Ron only. Say whether the fairness rule is in.
- **Switches, all as documented:** `cc_mirror`, `appt_confirm`, `asks_to_cc`,
  `crew_link_text`, `qb_invoices`, `invoice_auto`, `office_machine_texts`,
  `after_hours_reply`, `material_to_supplier`, `bills_to_qb`, `bills_to_cc`,
  `noc_notarize` OFF; `esign_packet`, `text_clock`, `deed_request` ON. Monday
  21 Sep is the office's first lead day with `cc_mirror` OFF, so Copy for CC
  is the road unless Kevin flips it.

## The feeds at 8:30 AM ET

Calls last ingested Fri 8:04 PM; texts last Sat 3:14 AM; nothing stuck in
`sms_outbox` or `rep_email_queue`; `gcal_events` 6 live, 1 deleted, 0 errors;
0 failed machine emails in 24 hours; 0 handoffs ever pressed. Quiet, as a
Saturday morning should be. Recheck at 10 AM: an hourly CDR export with nothing
new by then is the worker, not the weekend.

## The first session on the new account

Read `CLAUDE.md`, `docs/GOSPELS.md`, `docs/LANES.md`, then this file. Add the
repos (`4-touch-autopilot-central-command`, `trureview-mobile`,
`liberty-command`) and the connectors before anything else. Do not re-apply
416 or 417. Do not repoint anything in Contractors Cloud. Everything Kevin has
to decide is above and in the last column of `docs/LANES.md`.
