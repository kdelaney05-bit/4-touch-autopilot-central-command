# Hand-off · 16 Sep 2026, launch day, evening

Kevin moved to a new Claude account tonight. This is the whole state of the day for the next session, in one place. Every repo is pushed (see "Where everything is"). Memory files on Kevin's PC (`~/.claude/projects/C--Users-kdela-OneDrive-Desktop-Commercial-Desk/memory/`) are keyed to the folder, not the account, so a session opened from `Desktop/Commercial-Desk` on this PC gets them back.

## What shipped today (all live)

- **Central Command** (this repo, GitHub Pages, push = deploy, cache-bust `?v=94` on index.html + every js import): The Village as a top tab, dense-but-bubbly, name tags; the Nag (every @tag = email + text with the link, migration 360 + `liberty-notify` on the box); email-only for Jess and Sam (361); group tags @sales / @supers / @everyone (362); the Jetstream rule "stay on the file" (363); office seats search the whole book (364); Enter posts + the 🎤 dictation box; the tab mark; global error toasts; the find-popup fix.
- **Films (the Ride-Alongs, `docs/ride-alongs.html`)**: live-screen tours `?demo=1&…&tour=<1|keys|office|crews|gio|super|chain>&auto=1&voice=1` (narrator GUY, en-US-GuyNeural via msedge-tts, recorded under `films/<film>/<n>.mp3`; after any caption change run `node tools/narrate.mjs <film>`), and mp4 films under `films/` played by `docs/film.html?f=<name>` (in-page player; three of them 404'd until 5:30 PM — fixed). Films: the-invoice, the-bills, what-youre-getting, put-it-to-bed, tell-the-team, supervisor-app, **the-paperwork** (Gio's, 2:18).
- **Graphics**: `docs/who-does-what.html` (per-person before/after), `docs/today.html` (rollout list), `docs/visuals.html`, **`docs/sign-everything.html`** (the customer's phone signing the whole packet — Kevin's ruling below).
- **Rep app 1.0.4 OTA `01a0abe1`** (published on Kevin's "Publish." ~4:45 PM): SEND TO SIGN on every filed proposal/estimate row (mints an `estimate_links` row for that proofs id, texts the estimate-view link), Text it now / Email it now after saving, SAY IT on Today, push tap opens the site.
- **NOT published, on main (wt-photos f479e1b)**: the typed estimate covers the pad's Property box (Gio's ask). Needs Kevin's next "publish" (`eas update --channel production` from main, runtime 1.0.4 only — both phones seen today are native build 116 = 1.0.4).
- **Supervisor app 1.1.0 build 4** VALID on TestFlight (crash fixed: two expo-font copies). Kevin's phone still said "Could not reach the live database" — edge logs show ZERO requests from the supervisor app; cause unknown; needs his exact words after sign-out/in.
- **Gio's practice files**: customers "Gio Calderin — Practice 1–4 (fence)" under Gio, his cell (386) 795-2111 + gio@ on each, jobs `cc_company_id 1461`, source app_live.
- **The CC documents pull** (`pull-cc-files.mjs --scope all`) runs on the box overnight (live → sold → all); log `backend/logs/cc-files-2026-09-16.log`. `lb-board-refresh` cron stays PAUSED (22 s every 2 min was timing out statements).

## Kevin's rulings today (verbatim where it matters)

- The Ride-Along is the named form for anything new: self-running, subtitles + voice, one link per person. Every important change ships with one. Gospel 31 (permanent, self-improving) and 32 (the machine babysits so the people hunt).
- Voice: Steve Jobs clone DECLINED. Narrator = Guy (Microsoft neural, free). Never a real person's voice passed off as them.
- Passwords by Kevin's hand only (set-seat-password.mjs on the box). Nothing ships to reps before "publish". Every outbound is a draft until "send" — today he said "go ahead and reply to everybody", so replies went out as him.
- **Quotes**: "It should be like open… send this one for signature to the customer. That's it." → SEND TO SIGN. "When you change a price, it should automatically send a new one" → the row for the new proposal carries the same button (auto-send not built).
- **Paperwork, final shape (6 PM)**: "when the customer signs/accepts the contract, I want them to accept everything." Anything that was wet-ink/notary (NOC, Hold Harmless, permit applications) becomes a NORMAL sign-here/date-here document in the same packet. No notary tabs, no DocuSign notary add-on. Eric and Travis (notaries) witness in person and print a normal copy. Applies to everyone.
- Gio (email, 4:40 PM): remove the house/Property box from the estimate form (done, unpublished); keep the adapted proposal as is; wants the disclosures in the one send (see paperwork).

## The DocuSign finding (important)

There is NO DocuSign integration key for the company account and never was. Gmail searched top to bottom: only Kevin's PERSONAL DocuSign account (welcome mail 3 Jun 2026; the Claude DocuSign connector = that account, 4 personal envelopes, 0 templates). PC `.env` has no `DOCUSIGN_*`; the box has only `DOCUSIGN_CONNECT_SECRET` (ours). The 242 paperwork rail idles for that reason. **The fastest path to "accept everything" does not need DocuSign at all**: our own signing link (319 packet, `esign_packet` ON, 337 contract sheet) already does one signature + a tap per document. Two settings turn it on for Eric and Travis: (1) let sales-seat links carry the packet (today owner/admin-sent links only, PR #407's gate), (2) `paperwork_forms` rows for NOC / HHA / permit apps: `signing` esign_notary → esign and `stage` permit_run → signing (330 set esign_notary on purpose; Kevin reversed it tonight). Then one real job with Kevin copied.

## Open threads with people (all as Kevin, from his Gmail)

- **Gio** (thread "Sending a proposal to sign", 1a0abe23101143d2): four emails today — practice files + steps, "it was not turned on", the paperwork answer, the film link. Asked him to confirm the disclosure set. Owed: the disclosures switch once Kevin says go; the pad change publish.
- **Jess + Sam** (thread "Every supplier, one list", 1a0ab536fac7c01c): their supplier map read back, three gaps asked (SRS / Iron World / Stephens / iDeal; Bello / La Fence / MK = crews?; who types Heritage). The Bills build belongs to the Commercial-Desk ride-along session ("Claudette" = Jess's own Claude doing QuickBooks entry).
- **Uvoice** (ticket T20260910.0005): Pro-Tech SMS campaign complete — (321) 783-1694 office DID (Jessica/Sam/Laura in ConnectUC, log out/in), (321) 783-1688 Jermey → Cloud Message, (321) 352-6955 Pro-Tech rail line → Cloud Message. Tokens for the two Cloud Message accounts still being generated; told Oliver to send them on the ticket. ⚠️ that ticket email carries two portal passwords in clear text — never copy them. When tokens land: the sms-rail worker holds ONE `CLOUDMESSAGE_TOKEN`; needs a per-line token map before Jermey's `reps.sms_from='+13217831688'` and the Pro-Tech line ride the rail.
- **Village help desk**: scheduled task `village-help-desk` (every 10 min while the app is open) answers technical questions signed "— Kevin's help desk (automatic)".
- Awaiting Kevin: his cell for the nag; ruling "@Jessica alone = Jess Coley?"; Travis's call-sheet wording; an Oasis test text; phones for Laura, Jonathan, Obed, Robert, Nick.

## Owed builds (in order)

1. Publish the pad change (1.0.4 OTA from main).
2. Paperwork "accept everything": the two settings above + a phone-app Ride-Along frame for SEND TO SIGN (needs a real rep session to photograph).
3. Per-line Cloud Message tokens on the sms rail (when Uvoice sends them).
4. Supervisor app: customer texts in the app; the "could not reach the live database" mystery on Kevin's phone.
5. "Step off this file" button (363), Village name-tag bing card polish, job_progress timeout hog, gmail_ingest_record 403 from the box worker, Jess's office-line text copies, `lb-board-refresh` re-arm when Liberty Black returns.
6. The Bills / The Invoice (other session's design; Kevin's supplier map is the input).

## Where everything is

- **This repo** `kdelaney05-bit/4-touch-autopilot-central-command` main @ the commit that carries this file. Pages: https://kdelaney05-bit.github.io/4-touch-autopilot-central-command/
- **trureview-mobile** (`kdelaney05-bit/trureview-mobile`): main @ f479e1b (App.tsx: SEND TO SIGN e9644fd, SAY IT 3c7dcf1, estimate send-now 0958e38, pad cover f479e1b; migrations 360–364 applied + registered, **next free = 365** — verify the live registry first). Worktree on Kevin's PC: `C:\Users\kdela\AppData\Local\Temp\claude\wt-photos` (branch main-docs tracking main, push with `git push origin HEAD:main`; has a REAL node_modules, so `eas update` runs from it). Migrations run with `LIBERTY_ENV_FILE=C:/Users/kdela/trureview-mobile/backend/.env node backend/scripts/run-sql-file.mjs <file>` (only the LAST statement's rows print; the PC `SUPABASE_SECRET_KEY` is INVALID — mint admin things on the box).
- **Commercial-Desk clone** (`C:\Users\kdela\OneDrive\Desktop\Commercial-Desk`, same repo): branch `claude/jake-sales-virus` pushed @ 4fe60f2 with `tools/ride-along/` (make.mjs, specs, img, shoot-supervisor.mjs), `tools/evolution-film/`, the 339 draft migration, launch.json. The mp4s are gitignored there; the finished ones live in this repo's `films/`. That branch is 24 behind main — merge or cherry-pick, don't rebase blind.
- **liberty-command** (PUBLIC Pages repo): c.html rebuilt afa53c3, pushed. Working tree has 19 uncommitted files from other sessions — look before committing; it is public, never PII.
- **The box**: `ssh root@178.156.252.178`, `/opt/liberty-command`, env `/etc/liberty-command.env` (never source it), run as `sudo -u liberty LIBERTY_ENV_FILE=/etc/liberty-command.env node …`; services `liberty-notify`, `liberty-sms`, `liberty-paperwork` (idle), timers incl. cc_incremental every 15 min.
- **Post-as-Kevin rail** (when browser clicks die): `insert into team_messages (room, author_id, author_name, body) values ('village','fa314b31-dac6-4666-8920-e95d471f5732','Kevin Delaney','…')` — triggers fire.
- **Gospels**: `docs/GOSPELS.md` (31, 32). Laws: language law, no failure words, never generate/type/email passwords, JS `String.replace` with `$'` corrupts files (use split/join), Bash heredocs with backticks eat code (Write scripts instead).

## ✅ RULED, 16 Sep ~6:30 PM — "So this is what we're going to go with, correct? I love it."

Kevin approved `docs/sign-everything.html` as THE design: one link, the proposal + our contract sheet, every form the address needs listed under it filled from the file, plain sign-here / date-here / initial spots (NOC, Hold Harmless, permit applications included, nothing notarized), one green ACCEPT & SIGN EVERYTHING, every signed PDF on the file, Sam's tile lights. Build it exactly as drawn, on our own signing link, for every seat. No DocuSign.

## ✅ BUILT + LIVE, 16 Sep ~7 PM — "let's just change this for everyone… one stop shop when the customer accepts"

- **Migration 366 `366_sign_everything_once` APPLIED + REGISTERED** (365 was taken live by another session as `365_supplier_bills` — **next free = 367**, verify the registry first). All 38 active `paperwork_forms` rows: `stage='signing'`, `signing='esign'`, `wet_ink_if_easement=false`; the old values kept in `signing_before_366` / `stage_before_366`.
- **estimate-view DEPLOYED (`--no-verify-jwt`)**: `packetOn()` = the `esign_packet` switch + a sender, no seat check (every seat's link carries the packet and the contract sheet); `/packet` swaps each document's `file_url` for a 10-minute signed link to the FILLED PDF in `paperwork_filled` when the file has one (325), else the blank catalogue PDF.
- Kevin's test file (Kevin Delaney — Laptop test (fence), Cocoa) lists: NOC Brevard · Hold Harmless City of Cocoa · Warranty Disclosure · (optional) Removal/Reinstall, Core Drill & Paver, Wood Fence, Survey — all tap-to-sign under the one signature.
- Still owed on this: the page (`liberty-command/e.html`) lists documents with a tap each and one signature; the per-form yellow SIGN/DATE/INITIAL spots drawn in the mock are not on the real page yet (the mock is the target). The filled NOC appears only once `paperwork-fill` has run for that file.


## ✅ BUILT + LIVE, 16 Sep evening — CONTRACT SIGNING AND AUTO WORKFLOW (Kevin's name for the thread)

Kevin, ~9 PM: "We don't need to sign the NOC. The customer will have to get the NOC… a building permit just needs to be DocuSigned, it does not need a notary… pull the NOC out… we'll email it to them… they need to get it notarized and signed in order to get inspections… for a fence I could care less, for a roof we'll push it harder… a campaign bugging the customer… click here to scan in… the sales guys don't need to go back at all." And: "On the Oasis folder we're just gonna have them sign and initial what's on the normal contract and then just initial that they received the terms and conditions. That's it."

- **Migration 367 `367_noc_the_customers_errand` APPLIED + REGISTERED.** NOC forms: `stage='notarize'`, `signing='wet_ink'` (off the link; permit apps, hold harmless, disclosures stay at signing/esign). `noc_handoffs` (one per customer+job, a token per handoff), `noc_nudge_plans` (23 rows: 1461 and 1560 gentle — day 0/3/7/14/21 texts, day 22 office push; 1563 hard — day 0/1/2/4/6/9/12 texts, rep pushed day 2 and 13, office day 6 and 13). A trigger on `paperwork_filled` (kind noc) starts the handoff when the file is signed (325 fills seconds after ACCEPT). `noc_handoff_email` queues `rep_email_queue` from the brand's sold_office seat (Sam; Oasis: Jessica Oasis) to the customer + the rep (company mailbox only) + the seat, the filled PDF attached (`meta.files`, the nurture worker's packet path). `noc_nudge_sweep()` on pg_cron every 30 min: quiet hours 9–20 NY, opt-out, a human owing an answer, stale steps (>3 days) skipped, texts from `brand_sms_lines` with campaign_ok, play `noc_nudge`. `noc_handoff_receive` settles the CONTRACT_DOC:noc ask via `ask_settle_as` (office seat), buzzes the office and the rep. `ask_settle` regenerated from live with two lines: the open NOC ask never holds a `requires_none_open` gate, and a gate that already opened its step never opens it twice. Switch **`noc_notarize` ON since 16 Sep ~10:45 PM** (Kevin: "turn on the NOC switch", flipped through automation_switch_set as his seat); while OFF a seat's Email it on the file still sends.
- **Migration 368 `368_oasis_signs_the_contract_alone` APPLIED + REGISTERED.** `paperwork_forms.except_brands`; warranty-disclosure not for 1560; Oasis core-drill sheet back to permit_run. Oasis link = the contract sheet alone. **Next free = 369** (verify the registry first).
- **Edge fn `noc-return` DEPLOYED (no JWT; the token is the gate)** via the Supabase MCP: GET the page (three steps + camera), GET /form (one-hour signed link to the filled NOC), POST /photo (multipart → private job-docs under <cc>/<project>/OFFICE/DOC/ → `noc_handoff_receive`).
- **Central Command v95:** the Paperwork card's NOC row (Email it · Resend · Copy the photo link · Upload; the NEXT line says where it stands), the Property card's NEXT, the Office room switch **The NOC to the customer**, the queue row for a NOC ask is never red, words.js, the demo (Kowalski's file), the Ride-Along `?tour=noc` (7 steps, Guy), docs/sign-everything.html rewritten for the rule, LANES, GOSPELS 34, README.
- **Walk-through file for Kevin:** "Delaney NOC walk, Kevin" (customer 20192d28…, job 10f2595b…, reporting_excluded, kdelaney05@gmail.com, (321) 446-9610, 280 Clearlake Rd Cocoa): estimate #1005, link https://lzegjjbkfuecrhdvlvay.supabase.co/functions/v1/estimate-view/RbJ6l87wZpq7vUhRgnGEBee4LL
- **SENT from Kevin's Gmail 16 Sep ~10:45 PM on his word ("send the draft"):** "New rule on contracts: they accept now, they sign everything once, and you never go back for paperwork" — to the seven reps, cc Gio, Mike, Robert, Jess, Sam, Laura.
- **Owed:** the "received the terms and conditions" initial on the Oasis sheet (liberty-command e.html + estimate-view /sheet); the per-form yellow spots on the real page; Oasis and Pro-Tech NOC texts wait on their 10DLC lines (the email and the fence line work today); a "Step off" for stale handoffs; the 16 NOC asks open before tonight have no handoff — the office presses Email it on each file when it wants the customer to do it.


## ✅ ~11 PM — NOTHING WAITS AT SIGNING (369 · 370, applied live), and the walk-through's lesson

Kevin: "We don't need the NOC to do the project anymore… nothing is held up. As soon as that contract comes in, all the way through… make sure the office knows the customer has been sent the NOC… CC me, Jess and Gio on all the emails we set up… show me what everyone gets."

- **369 `369_nothing_waits_at_signing`:** `ask_chain` row 1 (→ PERMIT) fires on the CONTRACT settle with no gate (was: every paperwork ask settled); roofing 49–52 (INTRO_CALL, MATERIAL) the same; new rows 86/87: fence and Oasis open MATERIAL on Jonathan at the signature. `fence_jobs_stamp_rule` + `fence_checklist_open`: a calculator job's material releases at the signature (custom/aluminum once the deposit is in; the office's "paperwork official" mark still works). `machine_email_watchers` (kdelaney05@gmail.com, jessica@, gio@) → `machine_email_cc()`, on the NOC email and the SIGNED note. `noc_handoff_email` pushes the office seat and the rep when it goes. `signed_set_the_visit` rewritten: "SIGNED: {name} — it's moving", what opened on whom, to the rep + Gio + watchers + Sam + Jonathan, the signed contract and the filled NOC attached (the worker's signed_visit path). `fence_packet_queue`'s lock line and `fence_release_state.locked_on` say signature, not paperwork.
- **370 `370_the_handoff_opens_at_the_signature`:** Kevin's walk-through at 6:49 PM signed and went quiet: parcel-lookup answered "no parcel matched 280 Clearlake Rd" and returned BEFORE its fill call, so no NOC filled, so 367's handoff (which started on the fill) never started. Now `fence_checklist_open` opens the handoff at the signature (file line, page exists), the email goes the moment a filled NOC exists (the fill trigger, the worker's fallback fill, or Fill the NOC on the Property card), the day-0 text waits for the email, and a handoff unfilled after 30 minutes pushes the office seat once ("press Fill the NOC"). `noc_handoff_for` carries `filled`. The walk-through file's handoff was opened by the migration and filled server-to-server at 7:06 PM (statutory NOC, owner from the file) → emailed 7:06 PM to Gio, Jess, Kevin, Sam (+ the customer address, Kevin's own), the first text on the 7:30 sweep.
- **parcel-lookup patched + redeployed:** the fill fires whether or not the county matched, kept alive with `EdgeRuntime.waitUntil`.
- **Central Command v96:** the Property card has Fill the NOC before the county answers; words.js says the new rule. docs/sign-everything.html §7: who hears it, the second they sign.
- **Drafts in Kevin's Gmail:** the note to Gio and Jess ("You're on every NOC email now — and nothing waits at signing"), unsent.
- **Owed:** the supplier send from the MATERIAL ask (Jonathan's PO step) is a human step — "however it gets sent to the manufacturer" is not a machine's yet; the Oasis sheet initial; the SIGNED note's "Get out there today" wording that went to Kevin at 6:49 PM was the old one. **Next free = 371** (verify the registry).


## ✅ ~11:45 PM — 371 Luis hears it; the calculator walk-through file

Kevin: "send me a contract we filled out using the fence calculator that I can sign and it goes all the way to Luis, to everybody that it's coming."

- **371 `371_luis_hears_it_at_the_signature` APPLIED:** the production watcher (Luis) is pushed "EXPECTED · {first}" at the signature and rides the SIGNED note; when the job came through Gio's calculator the SIGNED note carries the packet itself in `meta.files` (proposal, material order, drawing, county packet, the signature image) and says what the job is from the takeoff. A job without a packet keeps the worker's printouts. **Next free = 372.**
- **Walk-through file 2, a calculator job:** "Delaney calculator walk, Kevin" — customer 95c05b07-9bf6-4513-a595-9c2e8440c381, job 1a6aff77…, fence job 63f23382… (Gio's Practice 1 cloned: INSTALL 6' TALL VINYL FENCING (135 LF) WITH (1) 5' GATES, $4,032, stock, no deposit; the four packet files copied in storage under the new customer), estimate #1006, link https://lzegjjbkfuecrhdvlvay.supabase.co/functions/v1/estimate-view/Ayz3sUgNv9S1c7b5zthzgQoMHK. On his signature: PERMIT on Sam, MATERIAL on Jonathan with the order attached (the calculator release), EXPECTED to Luis, SIGNED note with the packet to the rep, Gio, Kevin, Jess, Sam, Jonathan, Luis; the NOC fills and goes to him with the photo link; the first text on the next half-hour sweep.

## Sam's iterations (her two replies on the team note, 16 Sep 6:55 and 7:05 PM; Kevin replied-all 7:30 PM: "you just wrote the next three iterations")

1. **The photo has to become a clear PDF.** Clerks accept a NOC only as a clear PDF; customers' JPEGs are awful. Next: noc-return turns the pictures into one letter-size PDF (pdf-lib, one page per image), checks sharpness in the browser (Laplacian variance) and asks for a retake when blurry, and tells the customer to use the phone's own document scanner (iPhone Notes · Google Drive). The office gets a PDF ready for Simplifile.
2. **On the lines.** The generated statutory NOC (Brevard · Indian River · Seminole) is typed on its own lines; Volusia and Flagler use the county acroform fields. Sam's rule: Brevard / Indian River / Flagler = parcel ID + address is enough; Volusia = the full legal (shrink to fit, or an exhibit page). The **hold harmless and the permit applications are not filled today** (paperwork-fill: "only the NOC fills") — the customer taps the blank county PDF on the link. Next: fill maps per HHA / permit app (name, address, description = "Fencing"), the signature placed, a stamped PDF on the file — the "yellow spots" work.
3. **Disclosures only when needed.** Wood disclosure only when the job has wood (fence_takeoff_for material / the estimate items) → a paperwork_forms rule column. Survey disclosure: when signed it means the customer has NO survey — esign_packet_signed must stop settling the survey ask on it; instead note the ask "no survey — quote it and confirm the price with the customer" and push Sam.


## ✅ ~9:45 PM — 376 BACK TO THE VISIT (Kevin, after the fine print: "let's go back to our original move")

The fine print, read from the counters' own documents: Brevard §22-706 makes the hold harmless a condition precedent to fence-permit issuance when the fence encloses a utility easement (the county's form is notarized); Melbourne Beach §7A-53(7)(b) wants the owner's affidavit before issuance; Sebastian wants the easement affidavit notarized and recorded; Indian River's hold-harmless covenant is recorded; Rockledge's fence handout and Orange City's checklist list the recorded NOC among the documents required (contrary to 713.135(1)(a), which they may not enforce); Indian River/Vero says "preferred with the application, required before first inspection"; six cities notarize the owner's signature on the permit application. The NOC itself is never an issuance condition by law (713.135(1)(a)); it gates inspections on jobs over \$5,000 (713.135(1)(c),(d)). Kevin's draft to Eric said 45 days for lien rights — corrected to 90 days from final furnishing (713.08); 45 days is the notice-to-owner clock for lienors not in privity. Replies sent to Eric and to the team as Kevin.

- **376 \`376_back_to_the_visit\` APPLIED:** ask_chain row 1 and roofing 49–52 back to "every paperwork ask settled → open" (the NOC counts again); rows 86/87 off; ask_settle without the NOC exclusion (the "a gate opens once" guard stays); fence_jobs_stamp_rule back to 336 (paperwork official releases material); fence_checklist_open no longer releases at the signature but still opens the customer's note; signed_set_the_visit = "SIGNED: {name} — set the visit" with the notary packet in meta.files (the filled/stamped town forms + the NOC + the calculator's packet) to the rep, Gio, watchers, Sam, Jonathan, Luis; noc_handoff_email = the "you're in" note (locked in, approved and processing, the notary forms attached, the rep sets a time or any notary + the photo link, not a reason for anything to wait on our side); noc_nudge_plans reworded ("reply with a time for {{rep_first}}, or any notary + a picture"); the switch note. **Next free = 377.**
- **Kept from tonight:** the watchers, Luis on the SIGNED note, the photo page (one clean PDF), the on-the-lines fill for 14 forms (checked), the survey-disclosure fix, the wood sheet only for wood.
- **The terms clause (for the next terms version — Kevin approves the words):** "Acceptance of this proposal on the link is the Owner's acceptance of this contract. The Notice of Commencement, the Hold Harmless and any county form the Owner must sign before a notary are formalities of the county's permit and inspection process. They are not conditions of this contract; the Owner's delay in signing them does not release the Owner from it, delay payment, or shift any cost to Liberty. Liberty will send a notary to the Owner or accept the forms from any notary."
- **Owed:** that terms version (liberty-command, versioned pointer); the Oasis sheet initial; the supplier send from the MATERIAL ask; the per-city issuance rules as rows (what each counter asks for at intake) — the facts above are in this note until then.

## ✅ ~10:45 PM — 378 WHAT THE COUNTER ASKS FOR (the four owed builds, done)

- **The terms version:** every terms page in liberty-command (`terms/fencing · liberty · oasis · protech · roofing.html`) carries "PAPERWORK AFTER YOU ACCEPT" under the printed carbon — the clause above, word for word, with "the contractor" for "Liberty" so Oasis and Pro-Tech read right. estimate-view `TERMS_VERSION_DATE = 2026-09-16-formalities`, deployed: every signature from now stamps that version; older rows keep `2026-09-15-carbon`. Kevin can change the words; a change is a new version string, never an edit in place.
- **Per-city issuance rules as rows (378):** `permit_jurisdiction_rules` gained `hha_gates_issuance` (easement · always · never), `hha_notarized`, `permit_app_owner_signs`, `permit_app_notarized`, `noc_asked_at_intake`, `intake_note`, `source`, `read_at`, `confirmed_by/at`. Rows: Brevard (county), Cocoa, Rockledge, West Melbourne, Melbourne Beach, Palm Bay, Malabar, Grant-Valkaria, Indian Harbour Beach, Indian River (county), Vero Beach, Sebastian, Orange City, Holly Hill, Palm Coast. `read_at` set only where the counter's own document was read tonight; the rest say "from Sam's tree" until she confirms. `counter_rule_for(customer)` feeds the Property card's new line "At the counter · Rockledge: …" (v98).
- **Vero Beach:** the permit-app row's note no longer says DocuSign allowed — Travis (8:10 PM): ink on paper, no digital signatures; the covenant row says the same.
- **The Oasis initial:** a paperwork row `terms-received-oasis` (kind disclosure, brand 1560 only, the Oasis terms page as its document). The Oasis link now shows one tap under the contract — "Terms & Conditions — received" — with the customer's initials in script; the record (`signature_documents`) carries the page's hash at that moment and the consent "I received the Terms & Conditions at the link shown. My initials confirm it." (estimate-view). Nothing else under the Oasis contract.
- **The supplier send:** `material_order_send(customer)` — from the ops seat (Jonathan), one email per supplier the calculator's products point at (`fence_products.supplier` → `fence_suppliers`, else by material: vinyl FIS, aluminum Ideal, chain link Merchants, wood Home Depot), the calculator's material order PDF attached, the bill of materials and the gates in the body, Gio and the watchers copied; once a day per supplier; wood refuses until the PERMIT ask is done (Gio, 14 Sep). The MATERIAL ask on the file has **Send to supplier** (v98); the ask is closed by the supplier's order number as before. Switch **The order to the supplier** (`material_to_supplier`, OFF): ON = the release itself sends it, unless Gio's approval or an unknown product is in play.
- **Next free = 379.**


## ✅ 17 Sep, morning — 385 THE NOC IS THE REP'S (the final answer)

Kevin, 17 Sep ~9 AM: "everything's being signed including the hold harmless, all counties, the office will have that. The only thing a rep will be responsible for is getting the NOC, and that's on them; if they want to DocuSign it that's fine, but I don't want it in our process. It's not a condition of the acceptance. I want everything moving." And: "the emails going out — one says Gio Calderin is a notary — remove that completely." And: "when we send the confirmation of the appointment, remind them to have a copy of their survey handy."

- **385 applied:** the chain is 369 again (row 1 and roofing 49–52 open PERMIT / INTRO CALL / MATERIAL at the contract settle, no gate; 86/87 on; the NOC ask never holds a gate); the material releases at the signature (custom/aluminum once the deposit is in); `open_signing_asks` puts the NOC ask on the REP with the new words, and the open ones moved; switch `noc_notarize` OFF, the fill trigger `paperwork_filled_noc_handoff` dropped, the two waiting handoffs stopped; `signed_set_the_visit` = "SIGNED: … — it's moving" (what opened on whom, the NOC is the rep's, the filled NOC and the calculator's packet attached); `noc_rep_nag_steps` + `noc_rep_nag_sweep` (pg_cron hourly at :20, 9–19 ET): the rep is pushed on days 2, 5, 9, 14, 21, 30 and emailed (Gio copied, the NOC attached) on 5, 14, 30; `appt_confirm_queue`'s booked text asks for the survey (fence; the switch is still OFF). **Next free = 386** (verify — siblings used 379–384 overnight).
- **Central Command v99:** the NOC row on the Paperwork card says "the rep's"; the Office room switch says why it is off; words.js, the Ride-Along `?tour=noc` (seven steps, the final rule), the demo; GOSPELS 34; sign-everything.html §4/§6/§7; LANES.
- **liberty-command e.html:** the signed receipt says "you're in: your permit is being pulled and your material is going on order; your rep will be in touch about the one county form that needs a notary."
- **The team note:** the draft "Re: New rule on contracts — the final answer, in one place" rewritten to this; Kevin sends it after the run-through.
- **Walk-through file 3:** "Delaney final walk, Kevin" — see the email to Kevin for the link.
